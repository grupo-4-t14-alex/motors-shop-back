import { IAddressRequest } from "./addresses.interfaces";

import { ICarCreateRequest, ICar, ICarUpdateRequest, IResultCars } from "./cars.interfaces";

import { iComment, iCommentReturn } from "./comments.interfaces"

import { IEmailRequest } from "./email.interfaces";

import { ILogin, ILoginReturn, ILoginRequest } from "./login.interfaces";

import { IUser, IUserCreateRequest, IUserUpdateRequest, IUserReturn } from "./users.interfaces"


export { 
    IAddressRequest,
    ICarCreateRequest,
    ICarUpdateRequest, 
    ICar, 
    IResultCars,
    iComment,
    iCommentReturn,
    IEmailRequest, 
    ILogin,
    ILoginReturn,
    ILoginRequest,
    IUser,
    IUserCreateRequest,
    IUserUpdateRequest,
    IUserReturn
}
